[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/team-task-manager-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/auth/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "backend"
